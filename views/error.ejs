<!DOCTYPE html>
<html lang="zh">
<head>
    <%- include('head', {title: message}); %>
</head>
<body>
    <h1><%= message %></h1>
    <% if(error.status) { %>
    <h2><%= error.status %></h2>
    <% } %>
    
    <% if(error.stack) { %>
    <pre><%= error.stack %></pre>
    <% } %>
</body>
</html>